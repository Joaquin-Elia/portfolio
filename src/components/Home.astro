---
import NavMenu from "./NavMenu.astro";
---

<section class="home">
	<div class="home-noise">
		<h1 class="home-h1">Front end Developer</h1>
		<NavMenu />
		<h2>
			Hola soy <strong>Joaquin Elia</strong>, un desarrollador web de <strong>Buenos Aires, Argentina</strong>, especializado en el <strong>front end.</strong> 
			Mi <strong>experiencia</strong> como freelancer, me ha permitido trabajar para distintos tipos de <strong>clientes y proyectos.</strong>
		</h2>
	</div>
</section>

<style>
	.home {
		height: 100dvh;
		background: url('../assets/homeBg.jpg') no-repeat;
		background-size: cover;
		overflow: hidden;
	}
	.home-noise {
		padding: 2rem 4rem;
		height: 100%;
		display: flex;
		flex-direction: column;
		justify-content: space-between;
		background: url('../assets/homeNoise.svg') no-repeat;
		background-size: cover;
	}
	.home-h1 {
		font-family: 'Mulish', sans-serif;
		font-size: clamp(3em, 8.4vw, 9.5em);
		line-height: clamp(3rem, 8vw, 9rem);
		letter-spacing: -1.7px;
		text-transform: uppercase;
		max-width: 60rem;
	}
	.home h2 {
		max-width: 43rem;
		font-size: clamp(1.2em, 3vw, 2em);
		line-height: clamp(1.6rem, 3.5vw, 2.7rem);
		text-wrap: pretty;
		color: #E4E4E4;
	}
	.home h2 > strong {
		color: #fff;
		font-weight: 600;
	}
	@media (max-width: 75rem) {
		.home-noise{
			padding: 2rem;
		}
	}
	@media (max-width: 53rem) {
		.home {
			background-size: 160% 100%;
			background-position-x: 55%;
		}
		.home-noise{
			padding: 0;
			justify-content: center;
			align-items: center;
		}
		.home-h1 {
			text-align: center;
			letter-spacing: 0;
			font-size: clamp(2.9em, 13vw, 8.5em);
			line-height: clamp(3rem, 12.4vw, 8.2rem);
		}
		.home h2 {
			color: #E4E4E4;
			padding: 0 4rem;
			position: absolute;
			bottom: 2rem;
		}
	}
	@media (max-width: 29rem) {
		.home h2 {
			left: 0;
			padding: 0 1rem;
		}
	}
</style>